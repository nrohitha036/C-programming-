#include <stdio.h>

int main() {
    int days, i, worked, totalWorked = 0;

    printf("Enter the number of days in the month: ");
    scanf("%d", &days);

    
    for(i = 1; i <= days; i++) {
        printf("Did you work on day %d? (1 = Yes, 0 = No): ", i);
        scanf("%d", &worked);
        if(worked == 1) {
            totalWorked++;
        }
    }

    printf("\nTotal number of days worked in the month: %d\n", totalWorked);

    return 0;
}
