#include <stdio.h>
#include <string.h>

int main() {
    char str[100];
    printf("Enter a string: ");
    scanf(" %[^\n]s", str); 

    printf("Number of characters: %ld\n", strlen(str));
    return 0;
}
